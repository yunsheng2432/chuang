name: Daily ProductHunt Fetch

on:
  schedule:
    # UTC 01:00 = 北京时间 09:00（北京时间 = UTC + 8）
    - cron: "0 1 * * *"
  # 支持手动触发（在 GitHub Actions 页面点 "Run workflow"）
  workflow_dispatch:
    inputs:
      date_override:
        description: "手动指定采集日期，格式 YYYY-MM-DD（留空则采集昨天）"
        required: false
        default: ""

jobs:
  fetch:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r producthunt-daily/requirements.txt

      - name: Run fetcher
        env:
          PRODUCTHUNT_TOKEN: ${{ secrets.PRODUCTHUNT_TOKEN }}
          WEBHOOK_URL: ${{ secrets.WEBHOOK_URL }}
          FETCH_DATE_OVERRIDE: ${{ github.event.inputs.date_override }}
        run: python producthunt-daily/main.py

      - name: Generate HTML dashboard
        run: python producthunt-daily/generate_html.py

      - name: Commit results
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "daily: ProductHunt top products update"
          file_pattern: "producthunt-daily/producthunt.db producthunt-daily/reports/ docs/"